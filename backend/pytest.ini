[pytest]
# Collect tests only from the tests/ package — scripts/ holds manual integration
# runners (python scripts/test_*.py) that are not pytest-compatible.
testpaths = tests

# pytest-asyncio >= 0.21 defaults to strict; declare it explicitly so the
# intent is clear: every async test must carry @pytest.mark.asyncio.
asyncio_mode = strict
